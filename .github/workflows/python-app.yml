name: python-app

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  setup:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Python 3.10
        uses: actions/setup-python@v2
        with:
          python-version: "3.10"

  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Navigate to directory with main.py and print current directory
        run: cd /home/runner/work/xongptsh/xongptsh; pwd; ls -la

      - name: Test with pytest
        run: |
          python /home/runner/work/xongptsh/main.py > test-output.txt || true  # Continue even if the test fails

      - name: Archive test output as artifact
        if: always()
        uses: actions/upload-artifact@v2
        with:
          name: test-results
          path: test-output.txt
